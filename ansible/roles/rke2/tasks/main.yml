---
- name: Validate RKE2 role inputs
  ansible.builtin.assert:
    that:
      - (rke2_token | default('') | length) >= 16
      - (rke2_token | default('') | regex_search('^CHANGE_ME')) is none
      - (rke2_version | default('')) is match('^v[0-9]+\\.[0-9]+\\.[0-9]+\\+rke2r[0-9]+$')
      - cluster_vip | default('') | length > 0
      - cluster_domain | default('') | length > 0
    fail_msg: "RKE2 role requires a vaulted token, pinned rke2_version, cluster_vip, and cluster_domain."
  no_log: true

- name: Create RKE2 config directory
  ansible.builtin.file:
    path: /etc/rancher/rke2
    state: directory
    mode: '0755'

- name: Render RKE2 server config
  ansible.builtin.template:
    src: config.yaml.j2
    dest: /etc/rancher/rke2/config.yaml
    mode: '0600'
    backup: true
  diff: false
  notify: Restart rke2-server
  when: inventory_hostname in groups['rke2_servers']

- name: Render RKE2 agent config
  ansible.builtin.template:
    src: agent-config.yaml.j2
    dest: /etc/rancher/rke2/config.yaml
    mode: '0600'
    backup: true
  diff: false
  notify: Restart rke2-agent
  when: inventory_hostname in groups.get('rke2_agents', [])

- name: Install RKE2 server
  ansible.builtin.shell: |
    set -euo pipefail
    curl -sfL https://get.rke2.io | sh -
  args:
    executable: /bin/bash
    creates: /usr/local/bin/rke2
  environment:
    INSTALL_RKE2_TYPE: server
    INSTALL_RKE2_VERSION: "{{ rke2_version }}"
  when:
    - inventory_hostname in groups['rke2_servers']
    - not ansible_check_mode

- name: Install RKE2 agent
  ansible.builtin.shell: |
    set -euo pipefail
    curl -sfL https://get.rke2.io | sh -
  args:
    executable: /bin/bash
    creates: /usr/local/bin/rke2
  environment:
    INSTALL_RKE2_TYPE: agent
    INSTALL_RKE2_VERSION: "{{ rke2_version }}"
  when:
    - inventory_hostname in groups.get('rke2_agents', [])
    - not ansible_check_mode

- name: Enable RKE2 service
  ansible.builtin.service:
    name: "{{ 'rke2-server' if inventory_hostname in groups['rke2_servers'] else 'rke2-agent' }}"
    enabled: true
  when: not ansible_check_mode

- name: Start RKE2 service asynchronously
  ansible.builtin.command: "systemctl start {{ 'rke2-server' if inventory_hostname in groups['rke2_servers'] else 'rke2-agent' }}"
  async: "{{ rke2_service_start_async_timeout | default(900) }}"
  poll: 0
  register: rke2_service_start
  changed_when: true
  when: not ansible_check_mode

- name: Wait for RKE2 service start command
  ansible.builtin.async_status:
    jid: "{{ rke2_service_start.ansible_job_id }}"
  register: rke2_service_start_result
  until: rke2_service_start_result.finished
  retries: "{{ rke2_service_start_retries | default(90) }}"
  delay: "{{ rke2_service_start_delay | default(10) }}"
  when: not ansible_check_mode

- name: Confirm RKE2 service is running
  ansible.builtin.service:
    name: "{{ 'rke2-server' if inventory_hostname in groups['rke2_servers'] else 'rke2-agent' }}"
    state: started
  when: not ansible_check_mode

- name: Wait for local RKE2 registration endpoint
  ansible.builtin.wait_for:
    host: "{{ node_ip | default(ansible_host) }}"
    port: 9345
    timeout: "{{ rke2_local_registration_timeout | default(600) }}"
  when:
    - inventory_hostname in groups['rke2_servers']
    - not ansible_check_mode
